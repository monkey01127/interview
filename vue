生命周期：

  new vue() =>  (before)created => (before)mounted => (before)updated => (before)destroyed
 
mvc

  MVC模式是单向绑定，即Model绑定到View，当我们用JavaScript代码更新Model时，View就会自动更新
  
mvvm

  MVVM模式就是Model–View–ViewModel模式。双向绑定, 它实现了View的变动，自动反映在 ViewModel
